#ifndef HARD_PROCESS
#define HARD_PROCESS

#define ARRAY_SIZE 100

long long hardProcess(long long n); // Neste caso será fazer um fatorial
long long fatorial(long long n);
void printaDArray(int arraySize, long long **matriz);
void printaArray(int arraySize, long long matriz[][arraySize]);

#endif /*HARD_PROCESS*/
